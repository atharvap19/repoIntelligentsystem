# Where Vite proxies /api during `npm run dev`.
VITE_BACKEND_URL=http://localhost:8000

# Override only if you serve the frontend without the dev proxy
# (e.g. a built bundle hitting the backend directly, which needs CORS on the API).
# VITE_API_BASE=http://localhost:8000
